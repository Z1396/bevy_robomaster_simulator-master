// ============================================================
// 模块名：outpost
// 作用：前哨站（Outpost）子模块入口
// 职责：声明前哨站子模块的全部子模块，并对外提供统一的预导入
//       接口（`prelude`）。前哨站是 RoboMaster 比赛中位于场地
//       中央的旋转装甲目标，双方各有一个，本子模块负责其从场景
//       装配到旋转仿真、装甲识别的完整流程。
// ============================================================

// 构造模块：识别场景中的前哨站根节点并装配核心组件
mod construct;

// 常量模块：集中管理前哨站仿真使用的物理常量
mod consts;

// 预导入模块：统一导出公共 API 并聚合插件组，供外部使用
pub mod prelude;

// 旋转控制模块：定义旋转方向与旋转控制器
mod rotation;

// 更新模块：定义核心组件并提供每帧旋转更新系统
mod update;
